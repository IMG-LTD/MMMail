# Allowlist for scripts/check-migration-naming.sh
# 列出 v2.1.3 命名规约修订前已存在、暂不回填 -- DESCRIPTION: / -- ROLLBACK: 注释的历史迁移文件。
# 新增的迁移**不允许**加入此列表；只对未来文件强制注释。
# 跳号也由本文件豁免（V3→V8、V10→V16、V18→V21 是 v2.1.x 期间删除冲突迁移留下的痕迹，不再回填）。
#
# 格式：每行一个文件名（V{N}__*.sql）。# 开头为注释。

# v1.0–v2.0 期老迁移（无注释）
V3__release_metadata.sql
V8__web_push_subscriptions.sql
V9__calendar_invitation_orchestration.sql
V10__drive_readable_share_e2ee.sql

# v2.1.0 期 platform 模块（无注释）
V16__platform_outbox_event.sql
V17__platform_job_run.sql
V18__v21_collaboration_write_runtime.sql

# v2.1.2 期遗漏 ROLLBACK 注释（已落库不再修改）
V32__feature_flags.sql

# 跳号豁免：v2.1.x 期间删除冲突迁移留下的间隙
GAP V3 V8
GAP V10 V16
GAP V13 V15
GAP V18 V21
