spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/mmmail?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: mmmail_app
    password: replace-with-db-password
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: replace-with-redis-password
      database: 0
      timeout: 5s

  kafka:
    bootstrap-servers: 127.0.0.1:9092

  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: replace-with-nacos-user
      password: replace-with-nacos-password
      discovery:
        namespace: public
        group: DEFAULT_GROUP
      config:
        namespace: public
        group: DEFAULT_GROUP

mmmail:
  jwt-secret: replace-with-32-plus-char-random-secret

notes:
  - 该文件只保留脱敏示例，不再存放任何真实基础设施凭据。
  - 所有数据库、Redis、Nacos、Kafka、JWT 配置必须通过环境变量或密钥管理系统注入。
  - MySQL root、业务库账号、Redis 密码、Nacos 服务账号等真实值必须在仓库外部保存并定期轮转。
