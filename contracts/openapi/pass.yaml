openapi: 3.1.0
info:
  title: MMMail Pass Public Share Contract
  version: v2-freeze
paths:
  /api/v1/public/pass/secure-links/{token}:
    parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
    get:
      summary: Read a public pass secure link
      responses:
        '200':
          description: ok
